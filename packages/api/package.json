{
  "name": "@cellbrane/api",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run build:all && npm run format:dts",
    "build:all": "npm run build:tsc && npm run build:rollup && rm -rf build/*",
    "build:tsc": "tsc --build",
    "build:rollup": "rollup --config rollup.config.js",
    "format:dts": "prettier --write *.d.ts"
  }
}
